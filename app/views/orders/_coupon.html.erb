<center>
  <%= form_for(@order, url: "/profile/orders/coupon") do |f| %>
    <%= f.select(:coupon_id, current_user.coupons.collect{|c| [c.show_coupon, c.id]}, {}, { :class => 'user_coupon_dropdown' }) %>
    <%= f.submit "Apply" %>
  <% end %>
</center>
